(()=>{const p=new URLSearchParams(location.search),theme=p.get('theme')||'sexisme',projectId=p.get('projectId')||'';const host=location.hostname.toLowerCase(),path=location.pathname.toLowerCase(),staging=host.includes('staging')||path.includes('/studio-meandyoutoo-staging/')||host==='localhost'||host==='127.0.0.1';const API=window.STUDIO_API_BASE||(staging?'https://studio-meandyoutoo-api-staging.osc-fr1.scalingo.io':'https://studio-meandyoutoo-api.osc-fr1.scalingo.io');const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function api(url,opt={}){const r=await fetch(API+url,{headers:{'Content-Type':'application/json'},...opt});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Erreur API');return d}function card(ch,ci,pr,pi){return `<article class="profile-excel-card"><div class="profile-excel-head"><div><small>Chapitre ${ci+1} · Profil ${pi+1}/3</small><h3>${esc(ch.title)}</h3></div><span class="profile-color" style="background:${esc(pr.color)}"></span></div><div class="field"><label>Titre du profil</label><input data-profile-title="${pr.id}" value="${esc(pr.title)}"></div><div class="field"><label>Résumé du profil</label><textarea rows="4" data-profile-summary="${pr.id}">${esc(pr.summary)}</textarea></div><div class="field"><label>Contenu détaillé du profil</label><textarea rows="10" data-profile-content="${pr.id}">${esc(pr.content)}</textarea></div><div class="profile-method"><strong>Seuil Excel :</strong> ${pr.scoring_min} → ${pr.scoring_max} · non modifiable</div></article>`}async function load(){try{const d=projectId?await api(`/api/projects/${projectId}/composer`):await api(`/api/catalog/themes/${theme}/template`);const chapters=d.chapters||[];document.getElementById('theme-name').textContent=(d.theme?.title||d.project?.theme_title||'Sexisme');document.getElementById('profiles-root').innerHTML=chapters.map((ch,ci)=>`<section class="profile-chapter"><div class="section-head"><div><h2 class="section-title">${esc(ch.title)}</h2><p class="section-desc">3 profils issus du référentiel Excel.</p></div></div><div class="profile-excel-grid">${(ch.profiles||[]).map((pr,pi)=>card(ch,ci,pr,pi)).join('')}</div></section>`).join(''); if(chapters.some(ch=>(ch.profiles||[]).length!==3)) throw new Error('Le référentiel doit contenir exactement 3 profils par chapitre.'); bind();const q=projectId?`?theme=${encodeURIComponent(theme)}&projectId=${encodeURIComponent(projectId)}`:`?theme=${encodeURIComponent(theme)}`;document.getElementById('back-link').href='composer.html'+q;document.getElementById('next-link').href='parametrage.html'+q;}catch(e){const a=document.getElementById('profiles-alert');a.hidden=false;a.textContent=e.message}}function bind(){document.querySelectorAll('[data-profile-title],[data-profile-summary],[data-profile-content]').forEach(el=>el.addEventListener('change',async()=>{const id=el.dataset.profileTitle||el.dataset.profileSummary||el.dataset.profileContent;const key=el.dataset.profileTitle?'title':el.dataset.profileSummary?'summary':'content';if(projectId){try{await api(`/api/projects/${projectId}/profiles/${id}`,{method:'PATCH',body:JSON.stringify({[key]:el.value})})}catch(e){alert(e.message)}}else{const store=JSON.parse(localStorage.getItem('studio_profile_overrides')||'{}');store[id]={...(store[id]||{}),[key]:el.value};localStorage.setItem('studio_profile_overrides',JSON.stringify(store));}}))}load()})();