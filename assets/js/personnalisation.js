(()=>{
  const p=new URLSearchParams(location.search),theme=p.get('theme')||'sexisme',projectId=p.get('projectId')||'';let active=Math.max(0,Number(p.get('chapter')||0)),chapters=[],project=null;
  const api=(url,opt={})=>window.StudioAPI.request(url,opt),$=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const colorRank=color=>{const c=String(color||'').toLowerCase();if(c.includes('ff847')||c.includes('ff84')||c.includes('b423')||c.includes('red'))return 0;if(c.includes('ffc')||c.includes('yellow'))return 1;if(c.includes('77cd')||c.includes('green'))return 2;return 3;};
  const q=chapter=>`?theme=${encodeURIComponent(theme)}&projectId=${encodeURIComponent(projectId)}&chapter=${chapter}`;
  function card(ch,pr,pi){return `<article class="profile-excel-card"><div class="profile-excel-head"><div><small>Profil ${pi+1}/3</small><h3>${esc(pr.title)}</h3></div><span class="profile-color" style="background:${esc(pr.color)}"></span></div><div class="field"><label>Titre du profil</label><input data-profile-title="${pr.id}" value="${esc(pr.title)}"></div><div class="field"><label>Résumé du profil</label><textarea rows="4" data-profile-summary="${pr.id}">${esc(pr.summary)}</textarea></div><div class="field"><label>Contenu détaillé du profil</label><textarea rows="10" data-profile-content="${pr.id}">${esc(pr.content)}</textarea></div><div class="profile-method"><strong>Seuil :</strong> ${pr.scoring_min} → ${pr.scoring_max} · non modifiable</div></article>`;}
  function renderNav(){$('profiles-catalog-summary').textContent=`${chapters.length} parties · Questions et profils réunis dans un même parcours`;$('profiles-chapter-nav').innerHTML=chapters.map((ch,i)=>`<article class="creation-chapter-item ${i===active?'is-active':''}"><div class="creation-chapter-head"><span><small>Partie ${i+1}</small>${esc(ch.title)}</span><strong>${(ch.profiles||[]).length}/3</strong></div><div class="creation-chapter-tabs"><a href="composer.html${q(i)}">Questions</a><button class="${i===active?'is-active':''}" data-profile-chapter="${i}" type="button">Profils</button></div></article>`).join('');document.querySelectorAll('[data-profile-chapter]').forEach(button=>button.onclick=()=>{active=Number(button.dataset.profileChapter);history.replaceState(null,'',`personnalisation.html${q(active)}`);render();window.scrollTo({top:0,behavior:'smooth'});});}
  function render(){const ch=chapters[active];if(!ch)return;const profiles=[...(ch.profiles||[])].sort((a,b)=>colorRank(a.color)-colorRank(b.color));$('profile-chapter-kicker').textContent=`Partie ${active+1} · Profils`;$('profile-chapter-title').textContent=ch.title;$('profile-chapter-desc').textContent=active===chapters.length-1?'Le scoring de cette dernière partie est volontairement plus strict lorsque les situations comportent des enjeux liés à la loi.':'Personnalisez les trois profils issus du référentiel Me&YouToo.';$('profiles-root').innerHTML=`<div class="profile-excel-grid">${profiles.map((pr,pi)=>card(ch,pr,pi)).join('')}</div>`;$('profile-sticky-label').textContent=`Partie ${active+1}/${chapters.length} · ${ch.title}`;$('back-link').href='composer.html'+q(active);$('questions-step').href='composer.html'+q(active);if(active<chapters.length-1){$('next-link').href='composer.html'+q(active+1);$('next-link').textContent=`Questions de la partie ${active+2} →`;}else{$('next-link').href=`parametrage.html?theme=${encodeURIComponent(theme)}&projectId=${encodeURIComponent(projectId)}`;$('next-link').textContent='Toutes les parties terminées · Paramétrer →';}renderNav();bind();}
  async function load(){try{if(!projectId){const created=await window.StudioProject.createOrResume(theme);location.replace(`personnalisation.html?theme=${encodeURIComponent(theme)}&projectId=${encodeURIComponent(created.id)}&chapter=0`);return;}const d=await api(`/api/projects/${projectId}/composer`);project=d.project;chapters=d.chapters||[];active=Math.min(active,Math.max(0,chapters.length-1));if(chapters.some(ch=>(ch.profiles||[]).length!==3))throw new Error('Le référentiel doit contenir exactement 3 profils par partie.');$('theme-name').textContent=project?.theme_title||'Autodiagnostic';$('profiles-catalog-title').textContent=project?.theme_title||'Autodiagnostic';await api(`/api/projects/${projectId}/progress`,{method:'PATCH',body:JSON.stringify({currentStep:'personnalisation'})});render();}catch(e){$('profiles-alert').hidden=false;$('profiles-alert').textContent=e.message;}}
  function bind(){document.querySelectorAll('[data-profile-title],[data-profile-summary],[data-profile-content]').forEach(el=>el.addEventListener('change',async()=>{const id=el.dataset.profileTitle||el.dataset.profileSummary||el.dataset.profileContent,key=el.dataset.profileTitle?'title':el.dataset.profileSummary?'summary':'content';try{await api(`/api/projects/${projectId}/profiles/${id}`,{method:'PATCH',body:JSON.stringify({[key]:el.value})});}catch(e){await window.StudioModal.alert({eyebrow:'Enregistrement du profil',title:'Modification non enregistrée',message:e.message,type:'error',confirmLabel:'J’ai compris'});}}));}
  document.body.classList.add('sidebar-collapsed');const collapseButton=document.querySelector('[data-sidebar-collapse]');if(collapseButton){collapseButton.setAttribute('aria-expanded','false');collapseButton.setAttribute('aria-label','Déployer le menu');collapseButton.setAttribute('title','Déployer le menu');}load();
})();
